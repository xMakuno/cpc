#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
int main(){
    int n, t, segs = 0;
    cin >> n;
    vector<int> v(n);
    for(int i = 0; i < n; ++i) cin >> v[i];
    for(int i = 1; i < n; ++i){

    }
    return 0;
}