#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef pair<int, int> pii;
int main(){
    int n, x;
    cin >> n >> x;
    vector<pii> v(1e9);
    cout << v.size() << endl;
    for(int i = 0; i < n; ++i){
        
    }
    return 0;
}