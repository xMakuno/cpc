#include<bits/stdc++.h>
using namespace std;
int main(){
    int n, q;
    cin >> n >> q;
    vector<int> nums(n,0);
    vector<int> queries(q,0);
    for(int i = 0; i < n; ++i){
        cin >> nums[i];
        // cout << nums[i] << " \n"[i == n - 1];
    }
    int m, a, b;

    for(int i = 0; i < q; ++i){
        cin >> m;
    }

    return 0;
}